{% extends "::base.html.twig" %}

{% block body %}
    {% if form is defined %}
        {{ form(form) }}
    {% endif %}
    {% if background is defined %}
        {{ background.src }}
    {% endif %}

{% endblock %}
